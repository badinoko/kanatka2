# Progress

## 2026-03-14

- Введен новый docs-bundle проекта: `CODEX.md`, `AGENTS.md`, `docs/project/overview.md`, `docs/project/startup.md`, `docs/project/progress.md`.
- Зафиксирован стартовый dashboard проекта и текущее состояние репозитория.
- `CLAUDE.md` выведен из роли основного рабочего регламента и заменен на `CODEX.md`.
- По коду уже присутствуют два важных недавних изменения:
  - series-aware fallback для частично потерянных лиц в серии;
  - score-overlay таблицей поверх фото вместо компактного бейджа.
- Открытые хвосты перенесены в dashboard:
  - судьба per-photo JSON;
  - удобная настройка путей и логов;
  - обработка серий, где лица не найдены на всех кадрах.
- Следующий логический шаг после проверки пользователем: продолжить реорганизацию выходных артефактов и конфигурации.
- Per-photo JSON sidecar переведен в режим `opt-in`: по умолчанию он не создается.
- Для совместимости оставлен резервный путь `paths.photo_metadata_dir`, если sidecar придется вернуть в одной временной папке.
- Листы теперь могут брать score-данные из общих `ser*_report.json`, без sidecar рядом с каждым фото.
- Выполнена очистка сгенерированных артефактов проекта: удалены старые фото/JSON-выводы, `poc_results`, runtime-мусор и старые рабочие папки.
- Пустая рабочая структура `workdir/*` восстановлена заново, чтобы проект можно было сразу запускать на чистом состоянии.
- Во время очистки был остановлен запущенный GUI-процесс `PhotoSelector MVP`, потому что он держал открытым `workdir/logs/photo_selector.log`.
- В `overview.md` добавлены новые отложенные задачи:
  - SSD-оптимизация по количеству записей на диск;
  - переработка score-table в более интуитивно понятный пользовательский вид.
- KAN-008 уточнен: будущая таблица на фото должна показывать оценки `1..5` и понятные подписи от `очень плохо` до `отлично`.
- KAN-002: добавлен presence fallback для серий без найденных лиц, чтобы не отбрасывать занятые кресла автоматически при сильном person-сигнале в центре кадра.
- В `overview.md` добавлен lock-zone с базовым алгоритмом отсеивания и оценки, чтобы дальше синхронизировать логику через один понятный блок.
- В `CODEX.md` зафиксированы правила: `progress.md` остаётся append-only, при росте примерно до 1000 строк архивируется целиком через `archive/`, из проекта больше ничего не удаляем.
- В репозитории создана папка `archive/` как постоянная точка для переноса устаревших документов, результатов чисток и любых данных, которые раньше могли быть удалены.
- Scorer переведен на базовое правило `есть человек -> кадр идёт дальше`, критерий количества лиц стал бинарным, добавлен отдельный канал видимости лица вместо старого скрытого штрафа за маски и очки.
- По реальному smoke на текущем наборе `INBOX`: обработано 25 серий, выбрано 9, отброшено 16, собран 1 лист; кейсы `SER010` и `SER018` всё еще уходят в `empty`, поэтому KAN-002 оставлен в `ACTIVE`.
- В `Lock Zone` уточнен шаг 6: это не про пустой кадр, а про случаи, когда человек в кадре есть, но лицо не распознано из-за экипировки, ракурса, блика, движения, перекрытия или ошибки модели.
- `Lock Zone` переработан по замечаниям пользователя: убран отдельный пункт про очки и маски, шаг 3 переписан от состояния головы, шаг про отсутствие найденного лица очищен от дублей с бликами и перекрытиями, улыбка вынесена в отдельный дополнительный плюс.
- В `Lock Zone` критерий перекрытия уточнен до формулировки `голова перекрыта посторонними предметами: да/нет`, а кейс `лицо не найдено моделью` вынесен в отдельную техническую оговорку, а не оставлен в таблице основных критериев.
- В `overview.md` добавлен плотный набор подзадач для KAN-002, чтобы следующая работа шла по конкретным подпунктам, а не по расплывчатой общей формулировке.
- Scorer, конфиг и debug-overlay переведены на новую head-centric модель: `person_present`, `head_sharpness`, `head_exposure`, `head_occlusion`, `overall_sharpness`, `smile_bonus`.
- Улыбка теперь считается только бонусом и не штрафует кадр при отсутствии или проблемах распознавания.
- После перекладки критериев юнит-тесты проходят: `python -m unittest discover -s tests -p "test_*.py"`.
- Повторный smoke на `INBOX` прошёл без падений, но результат по пустым сериям не изменился: обработано 25 серий, выбрано 9, отброшено 16, собран 1 лист; `SER010` и `SER018` всё ещё остаются в `empty`.
- Оценка перекрытия в коде уточнена до head-region: теперь она считается по расширенной зоне головы, а не по телу человека и не только по узкой зоне лица.
- Бонус улыбки переведён с грубой эвристики `mouth_ratio` на MediaPipe face blendshapes; геометрия рта оставлена только как fallback.
- После смены smile-сигнала тесты проходят: `python -m unittest discover -s tests -p "test_*.py"`; на реальных кадрах smile уже перестал быть массово нулевым и даёт осмысленные значения порядка `0.30-0.61`.
- Для одиночных кадров на кресельной канатке добавлен второй presence-сигнал по центральным ROI кресла (`chairlift presence fallback`) с более агрессивным HOG-поиском.
- После этого `SER018` и `SER010` поднялись из `empty` в `selected`; повторный smoke на `INBOX` дал: 25 серий, 12 выбранных, 13 отброшенных, 1 собранный лист.
- KAN-002 переведен в `REVIEW`: целевые проблемные серии больше не теряются, но нужна пользовательская проверка, не появились ли ложные срабатывания на реально пустых креслах.
- Сессия завершена через docs handoff: в `overview.md` и `startup.md` сохранены pending user checks по `SER010`, `SER018`, `SER013`, `SER014`, chairlift fallback и новой head-centric логике score, чтобы не потерять их в следующем окне.

## 2026-03-14 (kanatka2 — новая ветка)

- Проект скопирован в `kanatka2` для независимого развития параллельно с оригинальным `kanatka`.
- `CODEX.md` перенесен в `archive/`, создан новый `CLAUDE.md` как основной рабочий регламент.
- Все ссылки обновлены: `AGENTS.md`, `docs/project/*`, `skills/kanatka-docs-maintainer/SKILL.md`.
- Путь по умолчанию в GUI изменен с `Канатка` на `INBOX`.
- **Радикальное упрощение системы скоринга (KAN-010):**
  - Скоринг: 6 компонентов -> 3 (`person_present=40`, `sharpness=35`, `exposure=25`).
  - Удалены: `head_occlusion`, `overall_sharpness`, `smile_bonus`.
  - Удалена HOG-детекция людей (`analyze_presence`, `_analyze_chairlift_presence`).
  - Удалены: `estimate_smile_score`, `estimate_goggle_score`, `estimate_mask_score`, `estimate_occlusion_score`.
  - Удалена функция `infer_subject_presence` — теперь просто `len(faces) > 0`.
  - Удалены оба fallback-механизма: `_apply_series_face_fallback`, `_apply_series_presence_fallback`.
  - Удалены секции конфига: `modes`, `series_selection`, `presence_detection`.
  - Из GUI убран чекбокс "Строго учитывать лица сбоку".
  - Score overlay: 6 колонок -> 3 (Человек, Резкость, Свет).
  - Все тесты переписаны под новую 3-компонентную систему.
  - Lock Zone обновлён: 4 шага вместо 6.
- Известное ограничение: люди в полной маске+очки+шлем (лицо не видно MediaPipe) уходят в discard.
- Требуется проверка: прогон реальных фото через упрощённый пайплайн.
- **Замена HOG person detection на Haar upper body cascade:**
  - HOG (Histogram of Oriented Gradients) обучен на стоящих пешеходов — на канатке (сидящие люди) не работает совсем (0 из 17 пропущенных серий).
  - Haar upper body cascade (`haarcascade_upperbody.xml`) из OpenCV ловит сидящих людей.
  - Результаты прогона на тестовом INBOX (25 серий):
    - MediaPipe only: 4/25 selected.
    - HOG fallback: 8/25 selected.
    - **Haar upper body fallback: 22/25 selected** (3 discarded: 2 реально пустых кресла + 1 пропуск).
  - Параметры: `scaleFactor=1.05, minNeighbors=3, minSize=(60,60)`.
  - Известное ограничение: возможно 1 ложное срабатывание на пустом кресле (~4%), что значительно лучше потери реальных фото.
  - `self.person_detector` (HOG) заменён на `self.upper_body_cascade` (Haar) в `face_utils.py`.
- **Начало разработки GUI-расширений (KAN-012):**
  - Веб-просмотрщик серий (`series_browser.py`) — локальный HTTP-сервер для просмотра всех серий и «спасения» пропущенных фото.
  - Утилиты экспорта (`export_utils.py`) — ZIP-упаковка результатов с фильтром по дате, автосинхронизация листов в сетевую папку.
  - Интеграция в GUI — кнопки «Просмотр серий», «Упаковать ZIP», настройка сетевой папки.
  - Решение: веб-просмотрщик на stdlib `http.server` (без Flask/Bottle) — zero dependencies, открывается в браузере.
  - Архитектура: отдельные файлы `series_browser.py` и `export_utils.py`, интеграция через кнопки в `gui.py`.
- **Реализация веб-просмотрщика серий (`series_browser.py`):**
  - HTTP-сервер на stdlib `http.server` + `ThreadingHTTPServer`, порт 8787.
  - Главная: сетка карточек серий с превью, бейджами (Выбрано/Пустое), оценкой score.
  - Детальная: все фото серии, тип детекции (👤лицо / 🔍силуэт / ❌не обнаружен), кнопка «Спасти фото».
  - Полноэкранный просмотр по клику. «Спасти» копирует в `workdir/selected/`.
  - Тесты: 5 тестов в `test_series_browser.py`.
- **Реализация утилит экспорта (`export_utils.py`):**
  - `create_results_zip()` — ZIP на рабочий стол, фильтр по дате.
  - `sync_sheets_to_network()` — копирование листов в сетевую папку (только новые файлы).
  - Тесты: 5 тестов в `test_export_utils.py`.
- **Обновление GUI (`gui.py`):**
  - Кнопки: «Просмотр серий», «Упаковать ZIP».
  - Секция «Настройки»: автосинхронизация, поле сетевой папки.
  - Автосинхронизация после обработки и сборки листов.
- **Конфиг:** добавлена секция `network` с `output_path` и `auto_sync_sheets`.
- Визуальная проверка в браузере подтверждена: карточки серий, фото, кнопки «Спасти» — всё работает.
- Все 21 тест проходят.

## 2026-03-14 (фидбэк пользователя и доработки)

- **Фидбэк пользователя по GUI — критически важные замечания:**
  1. **Баг с путём папки:** при смене папки в GUI через «Выбрать» возникает ошибка «Расположение недоступно». Причина: `INBOX` — относительный путь, Windows не может его разрешить в file dialog. Исправлено: `_pick_folder()` теперь резолвит путь через `Path.cwd()`.
  2. **«Спасти фото» — неверная логика:** текущая кнопка просто копирует фото в `selected/`, но это не помогает типографии. Реальная потребность: когда в типографии обнаружена ошибка (пустое кресло вместо людей), оператору нужно **посмотреть соседние по времени фото** — до и после ошибочного кадра, чтобы найти правильных людей. Это не «спасение одного фото», а **временно́й браузер** с возможностью выбора нескольких фото для печати.
  3. **ZIP без фильтрации по дате в UI:** бэкенд поддерживает `date_from`/`date_to`, но в GUI нет календаря/фильтров. Нужны пресеты: «за сегодня», «за неделю», произвольный диапазон, «всё».
  4. **Архитектурная модель работы:** программа работает автономно на компьютере №1. Листы автоматически синхронизируются в сетевую папку на ноутбук типографии (компьютер №2). Если типография находит ошибку — нужен быстрый доступ к соседним кадрам по времени.
- **Новые пожелания пользователя (backlog):**
  - Сворачивание в трей вместо закрытия окна.
  - Скрытые настройки разработчика за паролем (admin mode).
  - Настройки параметров детекции серий в GUI (для полевой настройки).
  - Упаковка в EXE для удобного распространения.
  - Потенциальная интеграция LLM в будущем.
- **Правило документации (установлено пользователем):** каждый фидбэк, каждое решение, каждое открытие — немедленно фиксировать в overview.md и progress.md. Журнал прогресса заведён специально для этого.
- Исправлен баг: `_pick_folder()` в `gui.py` — теперь относительный путь `INBOX` резолвится в абсолютный через `Path.cwd() / path`.
- Переделан rescue workflow: вместо кнопки «Спасти фото» для одного снимка реализован **временно́й браузер** — показывает все фото из соседних серий (до и после), позволяет выбрать несколько и отправить их на печать (скопировать в selected + синхронизировать в сетевую папку).
- Добавлен UI фильтрации по дате в ZIP-экспорт: пресеты «Сегодня», «Эта неделя», произвольный диапазон, «Всё».

## 2026-03-14 (стратегия продукта и видение)

- **Видение продукта от пользователя (ключевые решения):**
  1. **Продукт на продажу.** Это не внутренний инструмент — софт должен выглядеть профессионально, иметь интуитивный интерфейс, удобные настройки, тултипы для нового администратора.
  2. **Веб-интерфейс как основной UI.** Вместо отдельных окон всё должно работать внутри одного приложения: sticky навигационная панель сверху, все страницы/разделы внутри. Без множества вкладок браузера и кнопок «назад/вперёд». App-like UX.
  3. **Верстка: Desktop Full HD.** Мобильная адаптация — отдельная задача за отдельную плату, если понадобится. Пока пишем для 1920x1080.
  4. **Настройки инженера (admin mode).** Глубокие настройки для полевой адаптации: ползунки, дропдауны, чекбоксы с подробными подсказками. Все параметры, которые мы крутили при отладке (scaleFactor, minNeighbors, minSize, пороги, веса), должны быть доступны администратору через GUI. Отдельная страница настроек.
  5. **Двухкомпьютерная архитектура:**
     - Компьютер №1 (основной, без оператора): запускает обработку, собирает листы, автосинхронизирует в сетевую папку.
     - Компьютер №2 (типографский ноутбук, i3/8GB, слабый): принимает листы, оператор работает здесь.
     - Оба в одной LAN, без файрволлов.
     - Возможно, нужна отдельная лёгкая программа-приёмник для типографского ноутбука.
     - Сетевую настройку (IP, порты, шары) сделает разработчик-заказчик на месте.
  6. **Программа работает автономно.** За основным компьютером никого нет. Запустил — и забыл. Программа мониторит папку INBOX и обрабатывает новые фото автоматически.
  7. **Тултипы и подсказки — обязательно.** Каждый ползунок, каждая настройка — с описанием что делает. Новый администратор должен учиться на ходу.
- **Архитектурные решения (принятые мной как «директором»):**
  1. Веб-интерфейс становится основным: sticky nav с разделами «Серии», «Настройки», «Экспорт». tkinter GUI становится launcher-ом, который стартует сервер и минимизируется.
  2. Haar cascade параметры (scaleFactor=1.05, minNeighbors=3, minSize=60) выносятся в config.json → секция `haar_cascade`.
  3. Страница настроек в веб-интерфейсе: все параметры из config.json группируются логически, с подсказками на русском.
  4. API: POST /api/settings для сохранения настроек, GET /api/settings для чтения.
- **Параметры для страницы настроек (полный список):**
  - Детекция серий: `max_gap_seconds` (макс. пауза между кадрами серии), `cooldown_seconds` (пауза между сериями).
  - Haar cascade: `scaleFactor` (1.01–1.30, шаг 0.01), `minNeighbors` (1–10), `minSize` (20–200 px).
  - MediaPipe: `min_face_confidence` (0.1–1.0).
  - Пороги: `min_person_confidence`, `min_head_sharpness`, `good_head_sharpness`, `min_frame_sharpness`, `good_frame_sharpness`, `target_head_brightness`, `head_brightness_tolerance`.
  - Веса скоринга: `person_present`, `sharpness`, `exposure` (сумма=100).
  - Листы: `photos_per_sheet`, `grid_columns`, `grid_rows`, размеры, качество.
  - Сеть: `output_path`, `auto_sync_sheets`.
  - Обработка: `resize_longest_side`.
- Реализована страница настроек инженера (`/settings`) в веб-интерфейсе.
- Sticky навигация: Серии | Настройки | Экспорт — всегда видна.

## 2026-03-15 (README и документация)

- Создан `README.md` — полноценное описание проекта в GitHub-стиле: возможности, архитектура, стек, структура, конфигурация, планы.
- Исправлен дубликат KAN-016 в `overview.md` (была задвоена строка «Сворачивание в системный трей»).
- **Найдена и исправлена причина бага «Расположение недоступно»:** батники `run_gui.bat` и `run_demo.bat` перезаписывали `HOME` и `USERPROFILE` на каталог проекта — это ломало Windows file dialog и навигацию. Удалено.
- Батники обновлены: убрано вредное переопределение `USERPROFILE`, убрано `HOME`.
- `runtime_env.py` очищен от лишней логики `HOME`/`USERPROFILE`.
- В `gui.py` все `Path.cwd()` заменены на `get_project_root()` — надёжнее для резолвинга относительных путей.
- **Автозапуск веб-интерфейса:** при старте GUI веб-сервер на порту 8787 запускается автоматически (через `root.after(500)`), браузер открывается сам.
- Заголовок окна обновлён: «PhotoSelector MVP» → «PhotoSelector — Канатка».
- **Фидбэк пользователя:** кнопка «Спасти фото» копирует без предупреждения — раздражает. «Просмотр серий» открывает новую вкладку при каждом нажатии. Временно́й браузер не очевиден.
- «Спасти фото» теперь показывает confirm-диалог перед копированием.
- «Отправить на печать» (batch) тоже показывает confirm с количеством фото.
- Кнопка «Рядом» добавлена прямо на карточки серий на главной — не нужно заходить внутрь серии, чтобы открыть временно́й браузер.
- Кнопка «Просмотр серий» в tkinter GUI больше не открывает новую вкладку каждый раз — проверяет, запущен ли уже сервер.
- **КРИТИЧЕСКИЙ БАГ НАЙДЕН:** старые серверные процессы на порту 8787 не убивались при перезапуске. Новый код не подхватывался — пользователь видел старый интерфейс без изменений. Исправлено: `start_browser()` теперь убивает старые процессы на порту перед запуском нового сервера (`_kill_old_server()`).
- **Фидбэк пользователя (критический):** документация не обновлялась должным образом. Пожелания и решения из чата не переносились в overview.md. Усилены правила в CLAUDE.md — раздел 5a «Документирование фидбэка».
- `overview.md` полностью переписан: добавлена секция Architecture, честный список «Чего не хватает для продукта», новые задачи KAN-025 (пагинация), KAN-026 (встроенное окно), KAN-027 (Play/Stop).
- Тесты: 24/24 проходят через `.venv/Scripts/python.exe`. Через системный Python cv2 не грузится — это не баг кода.

## 2026-03-15 (фидбэк после проверки веб-интерфейса)

- **Пользователь подтвердил:** настройки работают, временно́й браузер работает, batch-выбор работает.
- **Фидбэк по UI:**
  1. Фиолетовая кнопка «Посмотреть рядом» — плохой контраст, текст не читается.
  2. Настройки нужно запаролить (admin mode) — простой пароль (дефолт 1234), со сменой.
  3. Пагинация серий — нужна. Кнопки пагинации + «Загрузить ещё» в навигационной панели.
  4. Переключение размера карточек (2-3 вида) — в навигации.
  5. Логотип в навигации (кликабельный).
  6. В далёком будущем: темная/светлая тема, колокольчик уведомлений (v2.0).
- **Контекст:** INBOX — синтетические фото. Завтра ожидаются реальные данные (сотни фото с канатки).
- **SER018:** вероятно бракованное синтетическое фото.
- **Задание:** зафиксировать всё, сделать план, двигаться автономно по задачам.

## 2026-03-15 (реализация KAN-017, KAN-025, KAN-028, KAN-029)

- **KAN-029 DONE:** Контраст кнопки «Рядом» исправлен — фиолетовый (#8e44ad) → синий (#4a6fa5), жирный шрифт.
- **KAN-017 DONE:** Настройки запаролены. Реализация:
  - При клике на «Настройки» показывается модальное окно с вводом пароля.
  - Дефолтный пароль `1234`, хранится в `config.json` → `auth.settings_password`.
  - Авторизация через cookie `kanatka_auth` (token в памяти сервера).
  - Эндпоинты: `POST /api/auth` (вход), `POST /api/change-password` (смена пароля).
  - Кнопка «Выйти» на странице настроек. Смена пароля внизу настроек.
  - Сохранение настроек тоже требует авторизации.
- **KAN-025 DONE:** Пагинация серий — 20 серий на страницу. Кнопки навигации + «Загрузить ещё».
  - Отображение: «Стр. X из Y (N серий)» + номера страниц + «Далее» + «Загрузить ещё».
- **KAN-028 DONE:** Переключатель размера карточек в навигации (крупные/средние/мелкие).
  - Три кнопки в навбаре. Выбор сохраняется в `localStorage`.
- Логотип (эмодзи лыжника) добавлен в навбар рядом с названием.
- **KAN-024 DONE:** Автономный режим — мониторинг INBOX. Реализация:
  - Кнопка «Запустить» на главной странице серий запускает мониторинг папки INBOX.
  - Используется watchdog (уже был в проекте) для отслеживания новых JPG файлов.
  - Автообработка: группировка серий → анализ → скоринг → выбор лучших → сборка листов.
  - Индикатор в навбаре: зелёная точка «Мониторинг» при активном режиме.
  - Кнопка «Остановить» для остановки. Счётчик обработанных серий.
  - API: `POST /api/monitor` с action=start/stop/status.
- **KAN-003 DONE:** Score overlay с 3 колонками проверен — авто-масштаб шрифтов, контрастная палитра.
- **KAN-008 DONE:** Score в веб-интерфейсе отображается с 5-звёздочным рейтингом и подписями (Отлично/Хорошо/Средне/Слабо/Плохо). Шкала: ≥85→5★, ≥65→4★, ≥45→3★, ≥25→2★, >0→1★.
- Все 24 теста проходят.

## 2026-03-15 (актуализация документации, планирование)

- **Критический фидбэк пользователя — определение направления всего проекта:**
  - Пользователь — не программист, готовит инженера к поездке на горнолыжный курорт.
  - Нужны ДВА готовых EXE: основная программа + приёмник для типографии.
  - Основная программа должна быть **самостоятельным приложением** (pywebview), а не вкладкой браузера.
  - Нужен полный README с инструкцией развёртывания, гайд по настройкам.
  - Нужен симулятор камеры для E2E тестирования связки двух программ.
  - Всё должно работать «из коробки» — инженер не программист.
- **Явная инструкция пользователя:** в этой сессии код не писать, только актуализировать документы.
- **KAN-035 (NEW):** Укрепление CLAUDE.md — ВЫПОЛНЕНО.
  - Полная инвентаризация всех 16 модулей src/ с описаниями.
  - Добавлены: стек и зависимости, структура репозитория, описание config.json, ловушки, инструменты разработки.
  - Из 107 строк вырос до полноценного технического справочника.
- **Новые задачи добавлены в dashboard (overview.md):**
  - KAN-030: Встроенное окно (pywebview) для основной программы.
  - KAN-031: Программа-приёмник для типографии (отдельный EXE, для i3/8GB).
  - KAN-032: Улучшенный симулятор камеры для E2E тестирования.
  - KAN-033: Deployment README — инструкция развёртывания.
  - KAN-034: Подробный гайд по настройкам.
  - KAN-035: Укрепление CLAUDE.md.
  - KAN-036: Публикация на GitHub (badinoko/kanatka2).
- **Active To-Dos переработаны:** 3 приоритетных уровня вместо плоского списка.
- **startup.md полностью переписан:** актуальный handoff с планом следующей сессии, рекомендациями, ловушками.
- **overview.md обновлён:** Architecture, Product Vision, «Чего не хватает» — всё актуализировано.
- Пользователь хочет публиковать на GitHub под `badinoko/kanatka2` (публичный репозиторий).

## 2026-03-15 (KAN-030 + KAN-031 + KAN-019 — критический путь к поставке)

- **KAN-030 DONE:** Встроенное окно (pywebview) для основной программы.
  - Создан `src/app.py` — pywebview launcher (WebView2/Edge на Windows).
  - В `series_browser.py` выделена функция `start_server()` (без открытия браузера) из `start_browser()`.
  - `main.py` обновлён: команда по умолчанию теперь `launch_app` (pywebview), `gui` сохранён как fallback.
  - `run_app.bat` — батник для запуска через pywebview.
  - Окно: 1280x800, заголовок «PhotoSelector — Канатка», resizable, min 800x600.
- **KAN-031 DONE:** Программа-приёмник для типографии.
  - Создана директория `receiver/` с 4 файлами:
    - `receiver_app.py` — pywebview entry point, порт 8788.
    - `receiver_server.py` — HTTP-сервер (stdlib), страница с автообновлением.
    - `receiver_watcher.py` — SheetQueue + watchdog для мониторинга папки.
    - `receiver_config.json` — настройки (watched_folder, port, refresh).
  - При первом запуске — диалог выбора папки (tkinter fallback).
  - UI: сетка карточек с превью листов, полноэкранный просмотр по клику, auto-refresh каждые 3 сек.
  - `run_receiver.bat` — батник для запуска.
- **KAN-019 DONE:** Упаковка в 2 EXE через PyInstaller.
  - `build/kanatka.spec` — основная программа (+ models/).
  - `build/receiver.spec` — приёмник (без ML зависимостей).
  - `build/build.py` — скрипт сборки (`--main`, `--receiver`, `--all`).
  - Результат: PhotoSelector 192 МБ, KanatkaReceiver 40 МБ — оба EXE собираются успешно.
- Новая зависимость: `pywebview==6.1` (+ pythonnet, clr_loader, bottle, proxy_tools).
- `.gitignore` обновлён: `build/work_main/`, `build/work_receiver/` вместо `build/` и `*.spec`.
- Тесты: 34/34 (было 24, добавлено 10 новых).
- Все три задачи Приоритета 1 закрыты — критический путь к поставке пройден.
- **Баг авторизации в pywebview:** fetch POST /api/auth возвращал 200 + Set-Cookie, но pywebview/WebView2 не сохранял cookie из fetch-ответов. Решение: заменили fetch на HTML form POST + server-side 302 redirect — cookie гарантированно сохраняются при навигации.
- Добавлен крестик (×) на модалку авторизации.

## 2026-03-15 (упаковка, очистка, E2E тестирование)

- **Очистка проекта:**
  - `run_gui.bat` и `run_demo.bat` перенесены в `archive/` — больше не нужны.
  - Оставлены только `run_app.bat` (основная программа) и `run_receiver.bat` (приёмник) для разработки.
- **Инсталляторы Windows (Inno Setup):**
  - Установлен Inno Setup 6 через winget.
  - Созданы скрипты: `build/photoselector.iss`, `build/receiver.iss`.
  - Собраны инсталляторы: `PhotoSelector_Setup.exe` (53 МБ), `KanatkaReceiver_Setup.exe` (15 МБ).
  - Инсталляторы с русским языком, wizard «Next-Next-Finish», ярлыки на рабочий стол + Start Menu.
  - Регистрация в «Программы и компоненты» — удаление через стандартный механизм Windows.
- **Build script обновлён:** `build/build.py` теперь поддерживает `--installers` для сборки Inno Setup инсталляторов.
- **E2E тестирование:**
  - Создан `tools/e2e_test.py` — запуск обеих программ на одном компьютере.
  - Сценарий: симулятор камеры → incoming → PhotoSelector → sheets → KanatkaReceiver.
  - Режимы: `--fast` (быстрые задержки), `--headless` (без окон), `--duration` (время теста).
- `.gitignore` обновлён: `installers/` добавлен.
- 34/34 теста проходят.

## 2026-03-15 (архитектурная переоценка после новых пояснений автора проекта)

- **Новое пояснение автора проекта:** мощный нижний компьютер у принтера должен делать всю обработку; верхний компьютер в ящике только складывает фото с камеры в сетевую папку. Сервис верхнего компьютера пока вне текущего скоупа.
- Это вступило в конфликт с текущим описанием продукта в docs, где основная программа жила у камеры, а `receiver/` считался обязательным вторым приложением.
- Зафиксирована новая рабочая гипотеза:
  - основной продукт = одна processing-программа на нижнем компьютере;
  - `input_folder` может быть сетевой папкой от верхнего компьютера;
  - `receiver/` не удаляем, но считаем optional/legacy режимом до отдельного подтверждения пользователя.
- **Найден важный технический факт по scoring:** код уже извлекает `yaw/pitch/roll`, `ear`, `mouth_ratio`, локальную резкость и яркость лица, но итоговый `src/scorer.py` использует только 3 компонента: `person_present`, `sharpness`, `exposure`.
- **Следствие для качества отбора:** fallback-кадры без найденного лица могут выигрывать у face-кадров по общей резкости/яркости. Это видно, например, в `workdir/logs/ser001_report.json`, где fallback-кадры получают score ~95, а кадр с найденным лицом уходит ниже из-за слабой face-exposure.
- В `overview.md` добавлены новые задачи:
  - KAN-037 — переопределить целевую схему компьютеров и режим поставки;
  - KAN-038 — пересобрать score-модель поверх уже собранных face-метрик;
  - KAN-039 — продумать ambiguous-series при близких score;
  - KAN-040 — спроектировать retention/cleanup и health-check сервис без рискованных побочных эффектов;
  - KAN-041 — синхронизировать README с фактической архитектурой.
- Создан новый `CODEX.md` как короткий Codex-companion к `CLAUDE.md`, без подмены приоритета правил.
- Предварительная оценка идей из `docs/new_notes.txt`:
  - NTP-мониторинг полезен как health-check;
  - автоматическая правка системного времени из приложения рискованна и не должна быть дефолтом;
  - cleanup/retention нужен, но отдельный демон-процесс пока выглядит преждевременным усложнением.

## 2026-03-15 (дополнительное уточнение по ролям компьютеров)

- Пользователь получил уточнение от автора проекта:
  - есть ящик с фотоаппаратом и малым компьютером у верхнего конца канатки;
  - есть компьютер в помещении канатки, где садятся и выходят люди, там уже стоит принтер и печатают фотографии;
  - между компьютерами есть сеть, фотографии передаются по сети.
- Вывод: архитектура не «один компьютер вместо двух», а «два компьютера с очень разным назначением».
- Рабочая интерпретация:
  - верхний компьютер = capture/transfer node;
  - нижний компьютер = основной processing/printing node.
- Docs обновлены так, чтобы не путать физическое наличие двух компьютеров с обязательностью двух равноправных приложений.

## 2026-03-15 (рабочая топология двух узлов зафиксирована)

- В `overview.md` добавлена явная recommended topology:
  - камера -> верхний малый ПК -> общая сетевая папка -> нижний ПК у принтера;
  - вся основная бизнес-логика находится на нижнем ПК.
- Зафиксирована product boundary:
  - обязательная часть продукта = основной app на нижнем ПК + работа с сетевой входящей папкой;
  - верхний ПК пока не требует полноценного UI/score/print logic;
  - `receiver/` оставлен как legacy/optional mode.
- В `startup.md` добавлен практический вывод для следующих сессий:
  - рассматривать `paths.input_folder` как будущую сетевую папку нижнего ПК;
  - для архитектурных изменений перечитывать `src/watcher.py`, `src/config.json`, `src/scorer.py`.

## 2026-03-15 (подготовлен документ на согласование с автором)

- Создан `docs/project/author-review-2026-03-15.md`.
- Назначение документа:
  - показать автору проекта зафиксированную архитектуру двух узлов;
  - согласовать роль нижнего ПК как главного processing node;
  - согласовать nearby/rescue как штатную часть основной программы;
  - согласовать более умеренную философию score вместо текущего переупрощения и вместо слишком сложной старой схемы.
- В документ отдельно вынесены:
  - что считать хорошим кадром;
  - минимальный набор критериев следующей версии;
  - почему `smile` должен быть только слабым бонусом;
  - почему `receiver/` сейчас логичнее считать optional;
  - список вопросов автору на подтверждение.
- В `overview.md` добавлен KAN-042 на прохождение этого review draft с автором проекта.

## 2026-03-15 (подготовлен короткий checklist для автора)

- Создан `docs/project/author-checklist-2026-03-15.md`.
- Формат документа специально короткий:
  - вопросы с ответами вида `Да` / `Нет` / `Уточнение`;
  - без длинной технички;
  - подходит для быстрой переписки.
- В checklist вынесены 5 блоков:
  - архитектура;
  - логика отбора;
  - критерии score;
  - ручная проверка и nearby/rescue;
  - эксплуатационные вопросы.
- Цель checklist:
  - быстро убрать двусмысленность;
  - получить однозначные ответы до следующей волны кодовых изменений.
- В `overview.md` добавлен KAN-043 на прохождение checklist с автором.

## 2026-03-15 (разбор ответов автора на checklist)

- Ответы автора достаточно чётко подтвердили основные продуктовые решения:
  - главный продукт живёт на нижнем ПК;
  - верхний ПК у камеры в основном только передаёт файлы;
  - отдельный `receiver/` не нужен как обязательная часть поставки;
  - ключевая проблема сейчас — `score` и выбор лучшего кадра.
- По логике отбора подтверждено:
  - первый вопрос автоматики: кресло пустое или нет;
  - цель не “идеальное лицо”, а самый читаемый и удачный кадр серии;
  - нижняя часть тела может быть частично перекрыта;
  - горнолыжные очки/маски/шлемы являются нормой;
  - список явного брака в checklist автора устраивает.
- По score подтверждено:
  - текущих 3 компонентов мало;
  - улыбка должна быть только слабым бонусом;
  - при нескольких людях на кресле предпочтение должно отдаваться кадру, где оба/все читаются лучше;
  - более читаемые лица важнее “эффектности” кадра с сильным профилем.
- По ручному rescue подтверждено:
  - это штатная, но отключаемая часть основной программы;
  - нужен настраиваемый `delta_score`;
  - спорные серии нужно автоматически помечать на ручную проверку.
- Оставшиеся открытые вопросы:
  - как показывать оператору nearby/rescue, не блокируя очередь входящих серий;
  - точный UX показа “помощи оператора” только в сложных случаях;
  - автопечать нужна как переключаемая настройка;
  - cleanup daemon автор хочет обсуждать с точки зрения изоляции ошибок, вопрос не закрыт.
- В `overview.md` добавлены новые задачи:
  - KAN-044 — operator-flow без блокировки очереди;
  - KAN-045 — переключаемая автопечать;
  - KAN-046 — стратегия изоляции cleanup-подсистемы.

## 2026-03-15 (подготовлен decision-note по трём открытым вопросам)

- Создан `docs/project/decision-note-2026-03-15-queue-print-cleanup.md`.
- В документе по каждому из трёх вопросов даны:
  - вариант A;
  - вариант B;
  - вариант C;
  - рекомендуемый вариант.
- Зафиксированные рекомендации:
  - для operator-flow брать очередь ambiguous-series без остановки основного pipeline;
  - для печати брать переключаемый autoprint через настройку;
  - для cleanup брать не постоянный демон, а гибрид с отдельным subprocess worker.
- Логика рекомендации:
  - не блокировать throughput из-за человека;
  - не делать печать слишком жёсткой;
  - не раздувать архитектуру cleanup раньше стабилизации score.
- В `overview.md` добавлен KAN-047 на согласование этого decision-note.

## 2026-03-15 (подготовлен KAN-038 score design brief)

- Создан `docs/project/kan-038-score-design-brief.md`.
- Цель brief:
  - перевести обсуждение score из уровня “кажется, критериев мало/много” в implementation-oriented дизайн;
  - зафиксировать, какие сигналы уже есть в коде и как их использовать без нового ML-контура.
- В brief зафиксировано:
  - почему текущие 3 компонента недостаточны;
  - 3-слойная структура решения:
    - occupancy gate;
    - quality gate;
    - ranking score;
  - рекомендуемые компоненты следующей версии;
  - роль `delta_score` и `decision_state`;
  - ожидаемые зоны изменений в коде (`scorer.py`, `analyzer.py`, `selector.py`, `series_browser.py`, `config.json`).
- Ключевой акцент:
  - сначала переписать score;
  - только потом проектировать ambiguous queue и остальную инфраструктуру.
- В `overview.md` добавлен KAN-048 на использование этого brief как базы следующей реализации.

## 2026-03-15 (зафиксирован реальный workflow от заказчика)

- Пользователь созвонился с заказчиком и уточнил полевой сценарий.
- Подтверждено:
  - оператору внизу не нужен поток фотографий в реальном времени;
  - вниз прилетают не “живые фото для просмотра”, а листы, которые автоматически печатаются;
  - по этим листам сразу изготавливаются сувениры до приезда клиентов вниз;
  - nearby/rescue нужен не как обязательный live-stage, а как exception tool, если нужен более удачный кадр или найден явный промах.
- Это существенно меняет акцент продукта:
  - основной workflow = автоматический печатный конвейер;
  - операторский rescue = постфактум / по исключению.
- В canonical `decision-note-2026-03-15-queue-print-cleanup.md` внесены подтверждённые уточнения:
  - вариант B по ambiguous-series оставлен как рабочий;
  - спорные фото должны уходить из основного workflow и не задерживать формирование остальных листов;
  - `autoprint = on` трактуется как боевой режим по умолчанию.
- В `overview.md` добавлен KAN-050 на приведение продукта к реальному workflow заказчика.

## 2026-03-15 (подготовлен copy-paste startup prompt для новой сессии)

- Создан `docs/project/new-chat-prompt-2026-03-15.md`.
- Назначение prompt:
  - дать готовый стартовый текст для нового окна чата;
  - сразу направить следующую сессию в KAN-038;
  - не возвращаться заново к архитектурной путанице.
- В prompt явно разделено:
  - что уже можно начинать без новых ответов автора;
  - что пока не стоит реализовывать до закрытия внешних хвостов.
- В `overview.md` это зафиксировано как KAN-049 (DONE).

## 2026-03-15 (консолидация docs и новая трактовка operator UI)

- `docs/project/overview.md` ужат обратно до короткого дашборда задач и проблем.
- Создан `docs/project/roadmap.md` как единый развёрнутый продуктовый план вместо размазывания narrative по нескольким одноразовым файлам.
- `docs/project/startup.md` переписан под новую каноническую структуру чтения:
  - `overview.md`;
  - `roadmap.md`;
  - `startup.md`;
  - tail `progress.md`;
  - `kan-038-score-design-brief.md` для score-работ.
- `AGENTS.md`, `CLAUDE.md` и `CODEX.md` синхронизированы с новой структурой docs.
- Обсужденческие документы 2026-03-15 перенесены в `archive/project/2026-03-15/`:
  - `author-review-2026-03-15.md`;
  - `author-checklist-2026-03-15.md`;
  - `decision-note-2026-03-15-queue-print-cleanup.md`;
  - `new-chat-prompt-2026-03-15.md`.
- Временные пользовательские файлы с `!!!!!` намеренно оставлены в `docs/project/` без перемещения и без статуса canonical docs.
- По новой продуктовой трактовке зафиксировано:
  - GUI на нижнем ПК всё ещё нужен;
  - но не как live-stream всех фото;
  - а как control panel, exception console и test/print preview.
- Добавлен новый workstream:
  - KAN-051 — test mode с экранной эмуляцией печати, zoom и debug overlays без реальной печати.

## 2026-03-15 (разобраны комментарии автора в raw `decision_note_...!!!!.md`)

- Проверен временный файл `docs/project/decision_note_2026_03_15_queue_print_cleanup!!!!.md` с пометками автора.
- Важное: эти комментарии не отменяют прежние выводы, а делают их более определёнными.
- По operator queue подтверждено:
  - вариант A исключён;
  - вариант B оставлен как рабочий;
  - `delta_score` для вызова оператора должен настраиваться в settings;
  - спорные серии должны полностью уходить из текущего workflow до принятия решения по ним;
  - спорные серии не должны задерживать формирование остальных листов.
- По queue variant C подтверждено:
  - допустим как более поздний optional-режим, не как первый шаг.
- По cleanup подтверждено:
  - выбран вариант C;
  - cleanup должен жить как отдельный scheduled subprocess worker.
- Эти уточнения внесены в:
  - `docs/project/overview.md`;
  - `docs/project/roadmap.md`;
  - `docs/project/startup.md`.
- После фиксации содержимого временные `!!!!!`-файлы могут быть спокойно убраны из `docs/project` в архив.

## 2026-03-15 (installers разигнорены, старые сборки удалены, startup получил execution brief)

- Из `.gitignore` убрана строка `installers/`.
- Это сделано намеренно:
  - старые installers признаны устаревшими;
  - следующие сборки нужно иметь возможность коммитить и отдавать заказчику через GitHub.
- Текущие старые файлы удалены из локальной папки `installers/`:
  - `PhotoSelector_Setup.exe`;
  - `KanatkaReceiver_Setup.exe`.
- В `installers/` оставлен `.gitkeep`, чтобы папка жила в репозитории до следующей реальной сборки.
- В `docs/project/startup.md` добавлен `Execution Brief For New Chat`:
  - что читать;
  - какие допущения считать подтверждёнными;
  - в каком порядке реально идти по KAN-038 / KAN-039 / KAN-044 / KAN-045 / KAN-051.

## 2026-03-15 (KAN-038 → KAN-039 → KAN-044 → KAN-045 → KAN-051 реализованы)

- **KAN-038: Новая 3-слойная score-модель.**
  - `src/scorer.py` полностью переписан: occupancy gate → quality gate → ranking score (7 компонентов, сумма 100).
  - `src/analyzer.py` расширен: `detection_type`, `readable_face_count`, per-face `readability`.
  - `src/config.json`: новые `scoring_weights` (7 ключей), 12 новых `thresholds`.
  - `src/badge_utils.py`: 6 новых колонок overlay вместо 3.
  - Fallback-кадры больше не побеждают face-кадры (потолок 45 vs ~60-70 у лиц).
  - Тесты: `test_scorer_logic.py` (9), `test_presence_logic.py` (3), `test_badge_utils.py` обновлены.

- **KAN-039: decision_state + ambiguous_manual_review.**
  - `src/selector.py`: добавлена `_compute_decision_state()`, серии получают статус `auto_selected` / `ambiguous_manual_review`.
  - `src/watcher.py`: `ambiguous_total` в summary.
  - Новый тест-файл: `tests/test_decision_state.py` (6 тестов).

- **KAN-044: Exception-flow без блокировки автопечати.**
  - Ambiguous фото идут в `workdir/ambiguous/` вместо `selected/` → не попадают в sheet_composer.
  - `src/config_utils.py`: `output_ambiguous` в runtime dirs.
  - `src/series_browser.py`: фильтр "Спорные", кнопка "Подтвердить", API `/api/confirm-ambiguous`.

- **KAN-045: Autoprint on/off.**
  - `src/print_utils.py` (новый): `print_sheet()` через mspaint /p.
  - `src/watcher.py`: `_autoprint_sheets()` после compose, блокировка в test_mode.
  - `src/config.json`: секция `print` (autoprint, test_mode, printer_name).
  - UI: чекбоксы в настройках, индикатор в навбаре.

- **KAN-051: Test mode + sheets preview.**
  - `/sheets` — галерея собранных листов с превью, зумом, ручной печатью.
  - Навбар: "TEST MODE" жёлтым при test_mode=true.
  - API `/api/print-sheet` — ручная печать одного листа (блокируется в test_mode).

- Итого: 45 тестов, 11 файлов — все проходят.
- Новые файлы: `src/print_utils.py`, `tests/test_decision_state.py`.

## 2026-03-15

- **KAN-052: Первая реальная симуляция — критический баг группировки.**
  - Пользователь подготовил 273 фото в INBOX и запустил симулятор + мониторинг.
  - Обнаружено: `camera_simulator.py` использовал `shutil.copy2()`, сохраняя оригинальные mtime файлов.
  - Последствие: `group_files_by_time()` видела разрывы в часы/дни между файлами одной серии → каждый кадр = отдельная "серия" из 1 файла → нет реального отбора, пустые кресла попадают на листы.
  - Фикс: `shutil.copy2()` → `shutil.copy()` — теперь mtime = момент копирования, группировка работает корректно.
  - Также исправлен мониторинг: следил за INBOX вместо `workdir/incoming/` (несоответствие config.json).
  - UI: убраны упоминания "INBOX", надписи заменены на понятные русские ("Мониторинг активен", "мониторинг входящих фото").
  - Все 45 тестов проходят.

- **KAN-053: Полное тестирование 273 фото — 3 критических бага.**
  - Баг 1: Программа видела только JPG (38 из 273). Остальные 235 = PNG — игнорировались. Фикс: `list_jpeg_files` → `list_image_files` с поддержкой `.png`. Watcher и sheet_composer тоже обновлены.
  - Баг 2: `sheet_composer.py` строка 65 — `glob("*.jpg")` не видел PNG-файлы в selected/. Фикс: мульти-glob для jpg+jpeg+png.
  - Баг 3: Ambiguous серии (38 из 46!) отправлялись в `workdir/ambiguous/` вместо `selected/` → листы не собирались. Фикс: лучшее фото ВСЕГДА идёт в selected/, ambiguous — только UI-флаг.
  - Результат после фиксов: 47 серий → 46 отобрано, 1 пустая, **5 полных листов** (8 фото каждый).
  - Качество: Top scores 82-86, большинство 60-80. 3 фото с score < 20 (Haar fallback, без лица).
  - Все 45 тестов проходят.
