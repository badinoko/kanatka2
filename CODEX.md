# CODEX.md

Краткий рабочий companion для Codex в репозитории `kanatka2`.

Последнее обновление: 2026-03-16.

---

## 1. Приоритет

1. `AGENTS.md`
2. `CLAUDE.md`
3. `docs/project/overview.md`
4. `docs/project/roadmap.md`
5. `docs/project/startup.md`
6. `docs/project/progress.md`
7. Этот файл (`CODEX.md`)
8. `TZ_PhotoSelector.md`
9. Код и конфиги

Этот файл не заменяет `CLAUDE.md`. Он нужен как короткая памятка именно для Codex-сессий.

---

## 2. Что важно прямо сейчас

**Продукт — это одна программа (PhotoSelector).** Один компьютер, один EXE.

- `receiver/` — legacy-код, не часть продукта, не поставляется заказчику.
- `input_folder` может быть локальной папкой или сетевой (от верхнего компьютера с камерой).

---

## 3. Как подходить к задачам

1. Сначала зафиксировать решение в `docs/project/overview.md`, если меняется архитектура, приоритеты или продуктовая форма.
2. Длинные объяснения и этапы держать в `docs/project/roadmap.md`, а не раздувать `overview.md`.
3. Потом append-only запись в `docs/project/progress.md`.
4. Если меняется точка входа в следующую сессию, обновить `docs/project/startup.md`.
5. Только после этого делать кодовые правки, если направление уже определено.

---

## 4. Где сейчас главный риск

- Не в упаковке и не в базовом GUI.
- Главный риск сейчас в рассинхроне между фактическим workflow заказчика, настройками и описанием системы:
  - заказчик подтвердил grouping серий по времени создания файла;
  - в коде логика `delta_score` есть, но её обязательно нужно держать доступной из UI;
  - user-facing guides быстро устаревают и начинают противоречить фактическому поведению.

Если задача связана с «почему выбралась не та фотография», первым делом использовать `skills/kanatka-score-debug`.

---

## 5. Практический порядок работы

Для архитектурных задач:
- читать `CLAUDE.md`, `overview.md`, `startup.md`, tail `progress.md`;
- затем проверять фактическое состояние файлов, а не верить старым статусам в docs;
- не удалять `receiver/`, пока пользователь явно не утвердил новую схему.

Для задач по качеству отбора:
- смотреть `workdir/logs/ser*_report.json`;
- сравнивать `selected` с `rejected`;
- отделять проблему threshold tuning от problem scoring logic.

Для end-to-end проверки:
- использовать `skills/kanatka-pipeline-smoke`.

---

## 6. Что не тащить в первую очередь

- Не начинать с NTP, системного трея и системных индикаторов.
- Не строить отдельный cleanup daemon, пока не утверждён основной рабочий процесс.
- Не плодить новые skills без явной повторяющейся потребности.

Сейчас уже достаточно:
- `kanatka-docs-maintainer`
- `kanatka-score-debug`
- `kanatka-pipeline-smoke`
